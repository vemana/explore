name: galtonboard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  fixnum: ^1.1.0
  google_fonts: ^6.2.1
  inject_annotation: ^1.0.0-alpha.4
  quiver: ^3.2.2
  built_collection: ^5.1.1
  common_base:
    path: ../../common/base
  common_collect:
    path: ../../common/collect
  common_event:
    path: ../../common/event
  common_lint:
    path: ../../common/lint
  common_math:
    path: ../../common/math
  common_state:
    path: ../../common/state
  common_testing:
    path: ../../common/testing
  common_ui_widgets:
    path: ../../common/ui/widgets

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  inject_generator: ^1.0.0-alpha.4
  checks: ^0.3.0
  test: ^1.25.7

flutter:
  uses-material-design: true
  assets:
    - assets/fonts/Rajdhani/
